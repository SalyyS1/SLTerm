{
    "$schema": "../gen/schemas/desktop-schema.json",
    "identifier": "default",
    "description": "Minimal capability set for the SLTerm shell. All data access goes through the Go backend over loopback HTTP/WS, so the webview needs no filesystem or shell permissions of its own.",
    "windows": ["main"],
    "permissions": ["core:default"]
}
